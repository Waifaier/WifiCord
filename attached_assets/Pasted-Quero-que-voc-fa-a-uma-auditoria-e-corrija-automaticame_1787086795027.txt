Quero que você faça uma auditoria e corrija automaticamente meu projeto para que ele rode corretamente no ambiente do Replit e apareça na Preview/Webview.

NÃO quero apenas instruções. Quero que você ANALISE OS ARQUIVOS, FAÇA AS ALTERAÇÕES NECESSÁRIAS, EXECUTE O PROJETO E TESTE até a Preview funcionar.

IMPORTANTE:
- Não reescreva o projeto do zero.
- Não remova funcionalidades existentes.
- Preserve Node.js + Express + SQLite + Socket.IO.
- Preserve autenticação por sessão.
- Preserve frontend HTML/CSS/JavaScript.
- Não use localStorage para sessão.
- Não altere a identidade visual sem necessidade.
- Não fique me perguntando o que fazer: investigue e resolva.
- Se encontrar vários problemas, corrija todos.
- Depois de corrigir, execute o projeto novamente e confirme que está funcionando.

==================================================
PROBLEMA ATUAL
==================================================

O Replit informa que:

"O aplicativo está rodando"

mas a:

"Preview não está pronta"

O projeto funciona localmente em conceito com:

http://localhost:3000

mas preciso que ele funcione corretamente dentro do ambiente do Replit.

==================================================
OBJETIVO
==================================================

Quero conseguir clicar em Preview/Webview no Replit e visualizar meu aplicativo normalmente.

O servidor deve:

- escutar em 0.0.0.0
- usar a porta fornecida pelo ambiente através de process.env.PORT
- funcionar tanto no Replit quanto localmente
- servir corretamente os arquivos do frontend
- permitir conexão do Socket.IO
- manter sessões funcionando
- permitir chamadas REST do frontend

==================================================
VERIFIQUE server/server.js
==================================================

Procure a configuração atual de:

server.listen(...)

Garanta que ela seja equivalente a:

const PORT = process.env.PORT || 3000;
const HOST = '0.0.0.0';

server.listen(PORT, HOST, () => {
  console.log(`🚀 Servidor rodando na porta ${PORT}`);
});

Não use somente:

server.listen(PORT, 'localhost')

ou:

server.listen(PORT, '127.0.0.1')

porque isso pode impedir o Replit de acessar a aplicação.

==================================================
EXPRESS
==================================================

Verifique se o Express está servindo corretamente o frontend.

O projeto possui:

client/
  index.html
  css/
  js/

Garanta que exista uma configuração equivalente a:

app.use(express.static(path.join(__dirname, '../client')));

E que:

GET /

retorne:

client/index.html

Se necessário, configure corretamente o path usando:

path.join()

e não caminhos absolutos específicos do meu computador.

==================================================
SOCKET.IO
==================================================

O projeto usa Socket.IO.

NÃO remova:

io.engine.use(sessionMiddleware);

e:

initSockets(io);

Verifique se o Socket.IO está ligado ao MESMO servidor HTTP usado pelo Express.

A estrutura deve ser equivalente a:

const app = express();
const server = http.createServer(app);
const io = new Server(server, ...);

Depois:

io.engine.use(sessionMiddleware);

initSockets(io);

server.listen(...);

Não crie um segundo servidor HTTP separado.

==================================================
SESSÃO
==================================================

O projeto usa express-session.

Garanta que a sessão funcione no Replit.

Não remova a autenticação.

Se existir:

app.use(sessionMiddleware);

preserve a configuração.

Se for necessário adaptar cookie para o ambiente, faça isso sem quebrar localhost.

NÃO use localStorage para substituir a sessão.

==================================================
CORS / FRONTEND
==================================================

Como frontend e backend são servidos pelo mesmo servidor, prefira SAME-ORIGIN.

Não adicione CORS desnecessário.

Garanta que o frontend consiga fazer:

fetch('/api/...', {
  credentials: 'same-origin'
});

==================================================
PACKAGE.JSON
==================================================

Analise package.json.

Garanta que:

npm start

inicie o servidor correto.

Se não existir "start", adicione.

Exemplo:

"scripts": {
  "start": "node server/server.js"
}

Não altere dependências sem necessidade.

Verifique se todas as dependências usadas realmente estão no package.json.

==================================================
REPLIT
==================================================

Verifique se o projeto possui algum arquivo de configuração do Replit que esteja impedindo a Preview.

Se necessário, configure corretamente o projeto para usar a porta definida pelo Replit.

Se o Replit Agent tiver acesso a configuração própria do ambiente, use-a.

Não crie configurações desnecessárias.

==================================================
DATABASE
==================================================

O projeto usa SQLite/better-sqlite3.

NÃO apague o banco.

NÃO recrie tabelas destrutivamente.

Verifique apenas se o caminho do banco é relativo e funciona no ambiente do Replit.

Se houver um caminho absoluto apontando para outro computador, corrija para um caminho relativo ao projeto.

==================================================
TESTE AUTOMÁTICO
==================================================

Depois de fazer as alterações:

1. Execute:

npm install

se necessário.

2. Execute:

npm start

3. Verifique se o servidor realmente inicia.

4. Verifique qual porta foi usada.

5. Verifique se existe erro no terminal.

6. Teste:

GET /

7. Teste se o index.html é retornado.

8. Teste se arquivos:

/css/style.css
/js/auth.js
/js/app.js
/js/socket.js

podem ser carregados.

9. Verifique se o Socket.IO está disponível.

10. Verifique se a Preview/Webview consegue acessar a aplicação.

11. Se aparecer qualquer erro, CORRIJA e teste novamente.

==================================================
IMPORTANTE SOBRE PORTA
==================================================

NÃO fixe a aplicação exclusivamente na porta 3000.

Use:

const PORT = process.env.PORT || 3000;

Assim:

- Replit usa a porta fornecida pelo ambiente.
- execução local continua usando 3000.

O host deve ser:

0.0.0.0

==================================================
IMPORTANTE SOBRE O FRONTEND
==================================================

Não altere URLs para algo como:

http://localhost:3000/api/...

O frontend deve usar URLs relativas:

/api/auth/me
/api/auth/login
/api/auth/register
/api/friends
/api/servers
/api/messages/...

Isso é necessário para funcionar na URL pública do Replit.

==================================================
IMPORTANTE SOBRE SOCKET.IO CLIENT
==================================================

O cliente Socket.IO também deve conectar ao mesmo host da aplicação.

Não fixe:

http://localhost:3000

no frontend.

Use uma conexão relativa ao host atual, por exemplo:

const socket = io();

Assim ele funcionará tanto no Replit quanto localmente.

==================================================
RESULTADO ESPERADO
==================================================

Quando terminar, quero:

1. npm start funcionando.
2. Servidor escutando em 0.0.0.0.
3. process.env.PORT sendo respeitado.
4. Express servindo client/index.html.
5. CSS e JS carregando.
6. API funcionando.
7. Sessão funcionando.
8. Socket.IO funcionando.
9. Preview/Webview do Replit funcionando.
10. Login aparecendo na tela.

Se a Preview continuar sem funcionar, NÃO pare.

Continue investigando:
- porta
- host
- server.listen
- Express static
- HTTP server
- Socket.IO
- package.json
- configuração do Replit
- erros no console
- erros de runtime

até encontrar a causa.

==================================================
REGRA FINAL
==================================================

FAÇA AS ALTERAÇÕES DIRETAMENTE NO PROJETO.

NÃO ME DÊ UM TUTORIAL.

NÃO ME MANDE APENAS CÓDIGO PARA EU COPIAR.

NÃO PARE PARA PERGUNTAR.

RESOLVA.

Depois de corrigir e testar, responda somente com:

"Resolvido."

e uma lista curta dos arquivos que foram alterados e o resultado do teste da Preview.